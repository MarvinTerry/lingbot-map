# Outputs

这个目录预留给本地运行和未来服务端的生成物。

建议后续把下面这些内容统一放到这里：

- 抽帧目录
- `GLB` 导出结果
- 第一人称飞行视频
- 调试截图
- job 日志

当前仓库根目录里已经存在的一些 `mp4`、`glb`、`*_frames/` 我这次没有自动搬动，目的是避免打断你现有流程。后续在引入 `lingbot_map_server/` 时，再统一切到 `outputs/jobs/{job_id}/` 会更稳。
